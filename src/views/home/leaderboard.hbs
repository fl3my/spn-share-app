<h1 class="mt-3">Top 10 Donators</h1>
<div class="rounded shadow bg-light p-3">
    <p>For every item donated the user recieves 10 points. The top 10 users with the highest score are displayed below.</p>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Rank</th>
                <th>Full name</th>
                <th>Score</th>
            </tr>
        </thead>
        <tbody>
            {{#each users}}
                <tr class="{{#if this.isCurrentUser}}table-secondary{{/if}}">
                    <td>{{this.rank}}</td>
                    <td>{{this.fullname}}</td>
                    <td>{{this.score}}</td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</div>