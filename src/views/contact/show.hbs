<div class="rounded bg-light shadow p-3 mt-4">
    <a href="/contacts" class="btn btn-primary">Back</a>
    <h1>Contact</h1>
    <div class="mt-4">
    <p>{{contact.message}}</p>
    <p>From: {{contact.email}}</p>
    <p>Sent at: {{contact.sentAt}}</p>
</div>