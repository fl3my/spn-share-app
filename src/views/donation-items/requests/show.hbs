<h1 class="mt-3">View donation item request</h1>
<div class="rounded shadow bg-light p-3">
  <a
    href="/donation-items/{{request.donationItemId}}/requests"
    class="btn btn-primary mb-3"
  >Go back</a>

  <h2>Request Information</h2>
  <p><strong>Date Requested:</strong> {{request.dateRequested}}</p>
  <p><strong>Request Status:</strong> {{request.status}}</p>
  <p><strong>Delivery Method:</strong> {{request.deliveryMethod}}</p>
  <p><strong>Additional Notes:</strong> {{request.additionalNotes}}</p>

  {{#if (eq request.deliveryMethod "COLLECT")}}
    <h2>Collection Information</h2>
    <p><strong>Collection Address:</strong>
      {{donationItem.address.street}},
      {{donationItem.address.city}},
      {{donationItem.address.postcode}}</p>

  {{/if}}

  {{#if (eq request.deliveryMethod "RECIEVE")}}
    <h2>Delivery Information</h2>
    <p><strong>Delivery Address:</strong>
      {{request.address.street}},
      {{request.address.city}},
      {{request.address.postcode}}</p>
  {{/if}}

  <h2>Requested by</h2>
  <p><strong>Name:</strong> {{user.firstname}} {{user.lastname}}</p>
  <p><strong>Email:</strong> {{user.email}}</p>
  <p><strong>Phone:</strong> {{user.mobile}}</p>

  <h2>Accept this request?</h2>
  <form
    id="acceptForm"
    action="/donation-items/{{request.donationItemId}}/requests/{{request._id}}/accept"
    method="post"
  >
    <button type="submit" class="btn btn-success">Accept</button>
  </form>
  <div class="d-flex align-items-center mb-3">
      <i class="bi bi-info-circle me-2"></i>
      <div>After you accept the donation, the pantry will see that they have claimed the item.</div>
    </div>
</div>